//business logic

const countTo = document.getElementById("countTo").value
const countBy = document.getElementById("countBy").value

function counting(limit, interval) {
  if (!Number(input)) {
    return null
  }
  listOfOutput = []
  for (let index = 0; index <= limit; index += interval) {
  listOfOutput.push(index);
  }
  return counting;
};





//UI logic



